export interface ThemePalette {
  id: string;
  bgMain: string;
  bgSurface: string;
  border: string;
  textPrimary: string;
  textSecondary: string;
  textMuted: string;
  textGhost: string;
  accent: string;
  accentHover: string;
  success: string;
}